plugins {
    alias(libs.plugins.android.application)
    alias(libs.plugins.kotlin.android)
    alias(libs.plugins.kotlin.compose)
    id "org.jetbrains.kotlinx.kover" version "0.8.3"
    id "org.sonarqube" version "5.1.0.4882"
}

android {
    namespace 'com.example.koverdemo'
    compileSdk 36

    defaultConfig {
        applicationId "com.example.koverdemo"
        minSdk 30
        targetSdk 36
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }
    kotlinOptions {
        jvmTarget = '11'
    }
    buildFeatures {
        compose true
    }
}

sonar {
    properties {
        property "sonar.projectKey", "KoverDemo"
        property "sonar.projectName", "KoverDemo"
        property "sonar.host.url", "http://localhost:9000"
        property "sonar.token", "sqp_8c6178f34dc5b9bd151eef130e9b76661554350a"
        property "sonar.sources","src/main/java"
        property "sonar.language","kotlin"
        property "sonar.coverage.jacoco.xmlReportPaths", "$buildDir/reports/kover/report.xml"
    }
}

dependencies {

    implementation libs.androidx.core.ktx
    implementation libs.androidx.lifecycle.runtime.ktx
    implementation libs.androidx.activity.compose
    implementation platform(libs.androidx.compose.bom)
    implementation libs.androidx.compose.ui
    implementation libs.androidx.compose.ui.graphics
    implementation libs.androidx.compose.ui.tooling.preview
    implementation libs.androidx.compose.material3
    testImplementation libs.junit
    androidTestImplementation libs.androidx.junit
    androidTestImplementation libs.androidx.espresso.core
    androidTestImplementation platform(libs.androidx.compose.bom)
    androidTestImplementation libs.androidx.compose.ui.test.junit4
    debugImplementation libs.androidx.compose.ui.tooling
    debugImplementation libs.androidx.compose.ui.test.manifest

    testImplementation "androidx.test:core:1.7.0"
    testImplementation "junit:junit:4.13.2"

}